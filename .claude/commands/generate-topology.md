Generate or update a Draw.io topology diagram for: $ARGUMENTS

Follow `.agent/skills/drawio/SKILL.md` exactly for all visual style rules, XML structure,
device placement, label positioning, octet labels, tunnel overlays, and validation.

The `.drawio` file is the only required artifact. Do not export PNG.
